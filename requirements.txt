feedparser>=6.0
PyYAML>=6.0
anthropic>=0.40        # optional at runtime: heuristic mode runs without it
kubernetes>=30.0        # for the discover command (live cluster inventory)
fastapi>=0.110        # optional: web UI (releasesage.web)
uvicorn>=0.27         # optional: web UI server
